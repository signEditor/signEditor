import axios from 'axios'

//这里进行ajax请求
/**
 * example
 * export const test = () => {
 *      return dispatch => {
 *          axios.get().then(res => {
 *              const action = test(res.data)
 *              dispatch(action)
 *          })
 *      }
 * }
 */